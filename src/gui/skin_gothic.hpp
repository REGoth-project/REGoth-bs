#pragma once
#include <BsPrerequisites.h>

namespace REGoth
{
  namespace GUI
  {
    /**
     * Creates and registers skins for some controls to make them look like the original GUI.
     *
     * See the internals for a list of all skins registered by this function.
     */
    bs::HGUISkin getGothicStyleSkin();
  }
}
